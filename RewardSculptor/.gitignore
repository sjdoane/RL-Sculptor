# Secrets
.env
.env.*
!.env.example

# Adversarial-review scratch (repro harness + agent debris + workflow scripts — never committed)
scratch_*

# uv / Python
.venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Sculptor local data
kg/graph.db
kg/graph.db.merged
kg/pdfs/
runs/

# Local evidence / ingestion / metric-rebuild transcripts. These are
# reproducibility scratch outputs, not source inputs; leaving them unignored
# makes run_context.json report a dirty code tree even at a clean commit.
.fleaven*
.ingest*
.metric*
.pytest*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
