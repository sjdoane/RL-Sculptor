"""Shared reward primitives — populated in a later iteration.

When primitive reward components (tilt² penalty, quadratic height reward,
drift dead-zone, imitation exp-kernel, etc.) are extracted into reusable
JAX/NumPy functions, they live here. Currently empty.
"""
